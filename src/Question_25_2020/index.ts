import fullArrayOf2dArray from './4.fullArrayOf2dArray';
import sortVersion from './20.sortVersion';

export default {
  run: () => {
    // fullArrayOf2dArray.run(); // 4.求二维数组的全排列组合
    sortVersion.run(); // 21.versions 是一个项目的版本号列表，因多人维护，不规则，动手实现一个版本号处理函数
  },
};
